type Error = variant { NotFound : record { msg : text } };
type Result = variant { Ok : vec record { text; nat32 }; Err : Error };
service : {
  get_question : () -> (text) query;
  get_votes : () -> (Result) query;
  reset_votes : () -> (Result);
  vote : (text) -> (Result);
}
